# Road Map UI

A simple drag and drop insertable list implemented in the functional Elm programming language.

To play with demo navigate to downloaded project directory and run:

```
npm install
npm start
```
